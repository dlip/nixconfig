format = """
$directory$git_branch$git_status$nodejs$rust$golang$php
$hostname$time$character"""

[hostname]
ssh_only = false
format = '[$hostname ]($style)'
disabled = false

[time]
disabled = false
format = '[$time ]($style)'
time_format = '%T'
